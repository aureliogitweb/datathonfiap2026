# Compativel com Python 3.11 e 3.12.
# Faixas em vez de pinos exatos: xgboost 3.3+ exige Python >= 3.12 e
# quebraria a instalacao em 3.11. Testado nas duas versoes — resultados
# identicos (logistica PR-AUC 0.557, Brier 0.107).

# Analise e modelagem
pandas>=2.2,<3
numpy>=1.26,<3
scipy>=1.11

# PINADO: o artefato app/artifacts/modelo_risco.joblib foi serializado
# com esta versao. Se alterar aqui, rode o notebook 03 ate o fim para
# regerar o .joblib — senao o app falha ao carregar.
scikit-learn==1.8.0

# Modelos challenger (comparacao no notebook 03)
xgboost>=2.1,<3.3
lightgbm>=4.5

# Visualizacao
matplotlib>=3.8
seaborn>=0.13

# I/O
openpyxl>=3.1
pyyaml>=6.0
joblib>=1.3
pyarrow>=14          # opcional: sem ele, salvar() cai para CSV

# Notebooks
jupyterlab>=4
ipykernel>=6

# App (ver tambem app/requirements.txt, usado no deploy)
streamlit>=1.30
